name: dotnet project
on:
  push:
    branches:
      - develop
jobs:
  dotnet-publish:
    runs-on: 'ubuntu-latest'
    steps:
      - name: code checkout
        uses: actions/checkout@v5
      - name: this is my dotnet project
        run: dotnet publish -c release src/Presentation/Nop.Web/Nop.Web.csproj -o demo
